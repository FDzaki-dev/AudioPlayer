package com.rudi.audioplayer.ui.theme

import androidx.compose.ui.graphics.Color

// "Default" — a calm, neutral slate palette. Deliberately the least stylized
// of the five: soft charcoal instead of true black, muted steel-blue accent,
// no dramatic identity — the safe, comfortable option everyone starts on.
val Slate = Color(0xFF1A1C1F)               // background
val SlateSurface = Color(0xFF232629)        // cards, sheets, mini player
val SlateSurfaceVariant = Color(0xFF2E3134) // dividers, unfilled tracks, skeletons
val SteelBlue = Color(0xFF8FA8BE)           // primary accent — dusty, muted steel blue
val SlateText = Color(0xFFDCDAD6)           // primary text — soft warm-grey white, not stark
val SlateMist = Color(0xFF8B8F94)           // secondary text — muted grey

// "Ink & Brass" — a boutique hi-fi palette: warm charcoal with a soft brass
// accent. Softened from its original near-black/near-white extremes — still
// warm and boutique, just easier on the eyes for long listening sessions.
val Ink = Color(0xFF1C1815)               // background
val InkSurface = Color(0xFF25201B)        // cards, sheets, mini player
val InkSurfaceVariant = Color(0xFF322B23) // dividers, unfilled tracks, skeletons
val Brass = Color(0xFFC9A96B)             // primary accent — softer, less saturated gold
val Parchment = Color(0xFFE8E1D3)         // primary text — warm off-white, not stark
val Mist = Color(0xFF9C9186)              // secondary text — warm grey

// "Midnight Bloom" — a jewel-toned nightlife palette: muted aubergine
// background with a dusty rose-mauve accent. Softened from its original
// vivid magenta/near-black extremes into something calmer and more wearable.
val Plum = Color(0xFF241E28)               // background
val PlumSurface = Color(0xFF2E2732)        // cards, sheets, mini player
val PlumSurfaceVariant = Color(0xFF3B333F) // dividers, unfilled tracks, skeletons
val Magenta = Color(0xFFB98CA0)            // primary accent — dusty rose-mauve, not neon
val Bloom = Color(0xFFE4DEE3)              // primary text — soft warm white
val Lavender = Color(0xFF9891A0)           // secondary text — muted dusty mauve

// "Paper & Ink" — a warm light theme: soft cream background, gentle espresso
// text, muted clay accent. Softened from its original near-white/near-black
// extremes into a genuinely gentle, paper-like feel rather than a stark page.
val Paper = Color(0xFFEDE6D9)               // background
val PaperSurface = Color(0xFFF5EFE4)        // cards, sheets, mini player
val PaperSurfaceVariant = Color(0xFFDDD3C0) // dividers, unfilled tracks, skeletons
val Terracotta = Color(0xFFB87B5C)          // primary accent — soft, muted clay
val InkText = Color(0xFF453F37)             // primary text — soft espresso brown, not black
val WarmGrey = Color(0xFF8A8070)            // secondary text — warm grey-brown

// "Botanical" — a quiet, nature-toned dark palette: muted forest background
// with a soft sage accent. Calm and organic, distinct in hue from the other
// four so the five themes genuinely feel like different choices, not
// variations on the same idea.
val Forest = Color(0xFF1C211D)               // background
val ForestSurface = Color(0xFF262C27)        // cards, sheets, mini player
val ForestSurfaceVariant = Color(0xFF333A34) // dividers, unfilled tracks, skeletons
val Sage = Color(0xFF93AD8D)                 // primary accent — soft sage green
val ForestText = Color(0xFFE1E4DC)           // primary text — soft off-white
val Olive = Color(0xFF8B9285)                // secondary text — muted olive-grey
